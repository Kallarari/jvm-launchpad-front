/* Aqui voce coloca os tipos (interfaces), do seu componente.
Esse e somente um exemplo, cada componente tem seu proprio arquivo de interface.
Coloque as tipagens lá para manter organizado.

export interface ExampleProps {
  id: string;
  name: string;
}

Ta comentado porque nao estou usando ele. 
*/