import type { NomeDoComponenteProps } from './interface';
import { ComponentContainer } from './styles';

export const NomeDoComponente = ({  }: NomeDoComponenteProps) => {
  return (
    <ComponentContainer>
      {/* Seu componente aqui */}
    </ComponentContainer>
  );
};